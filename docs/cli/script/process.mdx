---
title: '进程管理'
---

## 查看进程

```bash
arcadia ps
```

查看资源消耗情况和正在运行的代码文件进程，列出的资源包括CPU占用、内存占用、本地文件占用、进程占用
当检测到内存占用较高时会自动尝试释放缓存


## 清理进程

```bash
arcadia cleanup
```

默认杀死距离启动超过 `6` 小时以上的阻塞进程，通过此命令可以释放占用内存以维护项目稳定运行

```bash title="清理已卡死超过指定小时数的进程"
arcadia cleanup <hours>
```

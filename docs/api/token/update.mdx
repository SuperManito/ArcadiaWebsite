---
title: 更新
---
import { PUT } from '@/components/RequestTag';

<PUT>/token</PUT>

## 请求

<TypeTable
  type={{
    id: {
      description: '唯一标识',
      type: 'number',
      required: true,
    },
    name: {
      description: '配置名称',
      type: 'string',
    },
    expire_time: {
      description: '过期时间（时间戳），空为永久',
      type: 'number',
    },
    enable: {
      description: '状态',
      typeDescription: '1 启用，0 禁用',
      type: 'number<0 | 1>',
      default: 1,
    },
    permissions: {
      description: '访问权限，多个权限键用逗号拼接，不传则保持不变，具体请查看权限说明文档',
      typeDescriptionLink: '/docs/api/token/permission',
      type: 'string',
    },
  }}
/>

## 响应

参考[获取列表响应](/docs/api/token/list#响应)

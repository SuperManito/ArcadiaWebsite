---
title: 分页查询
---
import { GET } from '@/components/RequestTag';

<GET>/cron</GET>

## 请求

<TypeTable
  type={{
    page: {
      description: '指定页数',
      type: 'string',
      default: 1,
    },
    size: {
      description: '指定分页大小数量',
      type: 'string',
      default: 20,
    },
    type: {
      description: '任务类型过滤，当前固定分为 user 和 system',
      type: 'string',
    },
    search: {
      description: '搜索内容过滤，仅涉及任务名称和任务执行命令',
      type: 'string',
    },
    active: {
      description: '启用/禁用状态过滤',
      typeDescription: '1 启用，0 禁用，多个过滤用英文逗号分隔',
      type: "string<'0' | '1'>",
    },
    tags: {
      description: '任务标签过滤（仅限系统任务，取自 bind 值的中间字段，可通过 bindGroup 接口获取，支持多个用英文逗号分隔）',
      typeDescriptionLink: './bind-group',
      type: 'string',
    },
    orderBy: {
      description: '指定排序字段',
      type: "string<'sort' | 'last_runtime' | 'last_run_use'>",
      default: 'sort',
    },
    order: {
      description: '指定排序方式',
      typeDescription: '0 升序，1 降序',
      type: "string<'0' | '1'>",
    },
  }}
/>

## 响应

<TypeTable
  noRequiredMark={true}
  type={{
    data: {
      description: '数据项',
      type: 'object[]',
      required: true,
    },
    page: {
      description: '当前页码',
      type: 'number',
      required: true,
    },
    size: {
      description: '每页数据量',
      type: 'number',
      required: true,
    },
    total: {
      description: '数据总量',
      type: 'number',
      required: true,
    },
  }}
/>

数据项（主要为数据库 `tasks` 表字段）

<TypeTable
  noRequiredMark={true}
  type={{
    id: {
      description: '唯一标识',
      type: 'number',
      required: true,
    },
    name: {
      description: '任务名称',
      type: 'string',
      required: true,
    },
    type: {
      description: '任务类型，当前固定分为 user 和 system',
      type: 'string',
      required: true,
    },
    cron: {
      description: '定时规则（5位或6位的定时表达式）',
      type: 'string',
      required: true,
    },
    shell: {
      description: '执行的命令',
      type: 'string',
      required: true,
    },
    active: {
      description: '状态',
      typeDescription: '1 启用，0 禁用',
      type: 'number<0 | 1>',
      required: true,
    },
    config: {
      description: '高级配置',
      type: 'string<object>',
      required: true,
    },
    tags: {
      description: '任务标签',
      type: 'string',
      required: true,
    },
    last_runtime: {
      description: '最后一次运行的时间',
      type: 'string',
      required: true,
    },
    last_run_use: {
      description: '最后一次运行的时长（单位秒，精确到小数点后3位）',
      type: 'number',
      required: true,
    },
    sort: {
      description: '在分页中的位置序号（正整数），默认按照先后顺序递增',
      type: 'number',
      required: true,
    },
    create_time: {
      description: '任务创建时间',
      type: 'string',
      required: true,
    },
    remark: {
      description: '备注',
      type: 'string',
      required: true,
    },
    bind: {
      description: '后端用于关联底层内容的参数（仅系统任务使用）',
      type: 'string',
      required: true,
    },
    is_running: {
      description: '当前运行状态（任务是否正在运行）',
      type: 'boolean',
      required: true,
    },
  }}
/>

```json title="示例"
{
  "data": [
    {
      "id": 2,
      "name": "系统测试任务",
      "type": "system",
      "cron": "30 */2 * * *",
      "shell": "arcadia run example.js",
      "active": 1,
      "config": "",
      "tags": "",
      "last_runtime": null,
      "last_run_use": null,
      "sort": 1,
      "create_time": "2024-01-01 00:00:00",
      "remark": "",
      "bind": "",
      "is_running": false
    },
    {
      "id": 1,
      "name": "个人测试任务",
      "type": "user",
      "cron": "0 0 * * *",
      "shell": "arcadia rmlog",
      "active": 0,
      "config": "",
      "tags": "",
      "last_runtime": "2024-01-01 00:00:00",
      "last_run_use": 1,
      "sort": 1,
      "create_time": "2024-01-01 00:00:00",
      "remark": "",
      "bind": "",
      "is_running": true
    },
    ...
  ],
  "total": 5,
  "page": 1,
  "size": 20
}
```

默认倒序返回

---
title: 依赖管理操作结果
---

在操作依赖（安装、卸载、同步）后，返回依赖项数据用于实时更新

<Callout icon={false}>
接口路径：`/api/ws`  
事件名称：`depOperateResult`
</Callout>

<TypeTable
  type={{
    id: {
      description: '唯一标识',
      type: 'number',
      required: true,
    },
    status: {
      description: '状态',
      type: 'number',
    },
    installed_ver: {
      description: '已安装的版本',
      type: 'string',
    },
    last_error: {
      description: '最后一次错误信息',
      type: 'string',
    },
  }}
/>

根据业务场景返回部分字段，不返回完整记录数据。
